<ul class="nav">
  <li>
    <% if admin_signed_in? %>
      <%= link_to '首页', admin_root_path %>
    <% else %>
      <%= link_to '首页', root_path %>
    <% end %>
  </li>
</ul>

<ul class="nav" style="float:right">
  <% if admin_signed_in? %>
    <li>
    <%= link_to '登出', destroy_admin_session_path, :method=>'delete'%>
    </li>
  <% elsif store_manager_signed_in? %>
    <li>
    <%= link_to '登出', destroy_store_manager_session_path, :method=>'delete'%>
    </li>
  <% else %>
  <% end %>
  <% if admin_signed_in? %>
    <li>
      <%= link_to '修改账号', edit_admin_registration_path%>
    </li>
  <% elsif store_manager_signed_in? %>
    <li>
    <%= link_to '修改账号', edit_store_manager_registration_path%>
    </li>
  <% end %>
</ul>
